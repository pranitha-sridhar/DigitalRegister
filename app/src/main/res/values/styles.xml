<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">13sp</item>
        <item name="android:text">@string/app_name</item>
    </style>
    <style name="AppTheme.MyActionBar" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <!-- Customize your theme here. -->
        <item name="android:textStyle">bold</item>
    </style>
    <style name="MyTheme" parent="@style/Theme.AppCompat.Light">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">13sp</item>
        <item name="android:text">@string/new_register</item>
    </style>
    <style name="MainTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">13sp</item>
    </style>
    <style name="ColumnTheme" parent="@style/Theme.AppCompat.Light">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">13sp</item>
        <item name="android:text">@string/column</item>
    </style>
    <style name="animationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_right</item>
    </style>

</resources>